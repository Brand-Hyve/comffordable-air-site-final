# Comffordable Air Solutions

Client workspace. **Read `_profile/00-overview.md` first**, then the other `_profile/` files as
needed. Follow the read/write conventions in the root `../AGENTS.md`. Log meetings/decisions to
`_profile/log/`, keep tasks in `_profile/tasks.md`, never store secrets in `_profile/03-accounts.md`.

Deliverable: Astro site in this folder (live at comffordableairsolutions.com). Site NAP/config lives
in `src/config/business.json` — keep it in sync with the profile.
